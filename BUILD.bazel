load("@io_bazel_rules_go//go:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    # command = "fix",  todo should this be in here?
    external = "vendored",
    prefix = "github.com/margic/gobazel",
)
